interface:
  display_name: "CI Fix (Xcode-aware)"
  short_description: "Debug GitHub Actions with xcresult recovery"
  icon_small: "./assets/github-small.svg"
  icon_large: "./assets/github.png"
  default_prompt: "Use $gh-fix-ci to inspect the failing GitHub Actions checks on this PR, recover xcresult details for Xcode jobs when needed, summarize the root cause, and propose the smallest viable fix."
