interface:
  display_name: "Shared Craft Memory"
  short_description: "Bridge durable context between Codex agents on different hosts."
  default_prompt: "Use shared Craft context only for durable cross-host facts, verify current work in live threads and source systems, and publish a compact update only when another host will need it."
